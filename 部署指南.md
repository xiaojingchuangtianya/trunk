# 合金弹头游戏部署指南

## 系统要求

### 开发环境
- **Node.js** 14.0 或更高版本
- **Cocos Creator** 3.6 或更高版本
- **Git** (可选，用于版本控制)

### 运行环境
- **Windows** 10/11
- **macOS** 10.15+
- **Linux** (Ubuntu 18.04+)

## 安装步骤

### 1. 安装Node.js

#### Windows
1. 访问 [Node.js官网](https://nodejs.org/)
2. 下载LTS版本 (推荐v18.x.x)
3. 运行安装程序，选择默认选项
4. 验证安装：
   ```bash
   node --version
   npm --version
   ```

#### macOS
```bash
# 使用Homebrew
brew install node

# 验证安装
node --version
npm --version
```

### 2. 安装Cocos Creator
1. 访问 [Cocos官网](https://www.cocos.com/creator)
2. 下载Cocos Creator 3.6.x
3. 安装并启动

## 项目设置

### 1. 克隆或下载项目
```bash
# 如果项目已下载，跳过此步
# git clone [项目地址]
```

### 2. 安装服务器依赖
```bash
cd server
npm install
```

### 3. 启动服务器
```bash
# Windows
double-click start-server.bat

# 或手动启动
cd server
npm start
```

### 4. 打开Cocos Creator项目
1. 启动Cocos Creator
2. 点击"打开项目"
3. 选择项目根目录
4. 等待项目加载完成

## 场景配置

### 1. 创建基础场景
1. 新建场景命名为"GameScene"
2. 按以下结构创建节点层级：

```
Canvas
├── GameManager (空节点)
│   ├── GameManager.ts (脚本)
│   └── NetworkManager.ts (脚本)
├── PlatformContainer (空节点)
│   └── PlatformGenerator.ts (脚本)
├── PlayersContainer (空节点)
├── BulletsContainer (空节点)
└── GameUI (空节点)
    ├── Player1Health (Label)
    ├── Player2Health (Label)
    ├── GameStatus (Label)
    ├── RestartButton (Button)
    └── MenuButton (Button)
```

### 2. 创建预制体

#### 玩家预制体 (Player.prefab)
1. 创建空节点命名为"Player"
2. 添加组件：
   - Sprite (精灵组件)
   - Player.ts (脚本)
   - RigidBody2D (物理刚体)
   - BoxCollider2D (碰撞器)
3. 设置Sprite颜色：
   - 玩家1: 红色 (255, 100, 100)
   - 玩家2: 蓝色 (100, 100, 255)
4. 设置碰撞器：
   - Size: 40x60
   - Offset: 0x30
5. 创建子节点"BulletSpawn"在(20, 30, 0)

#### 子弹预制体 (Bullet.prefab)
1. 创建空节点命名为"Bullet"
2. 添加组件：
   - Sprite (精灵)
   - Bullet.ts (脚本)
   - RigidBody2D (刚体)
   - BoxCollider2D (碰撞器)
3. 设置Sprite：
   - 使用小圆形精灵
   - 颜色: 黄色 (255, 255, 0)
4. 设置碰撞器：
   - Size: 20x10

#### 平台预制体 (Platform.prefab)
1. 创建空节点命名为"Platform"
2. 添加组件：
   - Sprite (精灵)
   - BoxCollider2D (碰撞器)
3. 设置Sprite：
   - 使用方形精灵
   - 颜色: 棕色 (139, 69, 19)
4. 设置节点名称为"Ground"

### 3. 配置物理系统
1. 打开"项目设置" -> "模块设置"
2. 启用"Physics2D"模块
3. 设置重力: (0, -1000)

### 4. 设置相机
1. 选择Main Camera
2. 设置投影模式为"正交"
3. 设置正交高度: 500
4. 设置位置: (0, 0, 1000)

## 测试运行

### 1. 启动服务器
```bash
# 在项目根目录
cd server
npm start
```

### 2. 启动游戏
1. 在Cocos Creator中打开项目
2. 点击"运行"按钮
3. 测试本地双人游戏：
   - 玩家1: WASD + J/K
   - 玩家2: 方向键 + 1/2

### 3. 在线测试
1. 启动服务器
2. 打开两个浏览器窗口
3. 访问游戏URL
4. 测试在线对战功能

## 常见问题解决

### 问题1: npm命令未找到
**解决**: 确保Node.js已正确安装，重启命令行

### 问题2: WebSocket连接失败
**解决**: 
1. 检查服务器是否运行
2. 检查防火墙设置
3. 确认端口号(默认8080)

### 问题3: 物理引擎不工作
**解决**:
1. 检查Physics2D模块是否启用
2. 确认刚体和碰撞器设置
3. 检查重力设置

### 问题4: 角色无法移动
**解决**:
1. 检查输入系统设置
2. 确认脚本组件已添加
3. 检查刚体类型设置

## 性能优化建议

### 1. 对象池
- 使用对象池管理子弹
- 减少垃圾回收

### 2. 碰撞优化
- 使用分层碰撞
- 减少不必要的碰撞检测

### 3. 网络优化
- 使用二进制协议
- 实现预测和回滚

## 扩展功能

### 1. 添加新武器
- 创建新的子弹类型
- 添加武器切换系统

### 2. 添加道具
- 创建道具预制体
- 实现道具拾取和使用

### 3. 添加地图
- 创建多个场景
- 实现地图切换系统

## 技术支持

如有问题，请联系：
- 邮箱: [your-email@example.com]
- GitHub: [your-github-repo]
- 文档: [项目文档链接]