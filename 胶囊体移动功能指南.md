# 🎮 胶囊体左右移动功能指南

## 📋 功能概述

Capsule.ts提供了完整的胶囊体左右移动功能，支持键盘控制和自动移动两种模式。

## 🎯 使用方法

### 🔧 基础设置

1. **添加组件**：在Cocos Creator中，将`Capsule.ts`脚本添加到胶囊体节点上
2. **配置属性**：在属性检查器中调整以下参数

### ⚙️ 可调参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `moveSpeed` | Number | 200 | 手动移动速度（像素/秒） |
| `moveRange` | Number | 300 | 左右移动范围（像素） |
| `autoMove` | Boolean | false | 是否启用自动移动 |
| `autoMoveSpeed` | Number | 100 | 自动移动速度（像素/秒） |

### 🎮 控制方式

#### 手动控制（默认）
- **A键** 或 **←箭头** → 向左移动
- **D键** 或 **→箭头** → 向右移动
- **同时按住** → 停止移动

#### 自动控制
设置`autoMove = true`，胶囊体会自动在设定范围内左右往返移动。

### 📁 代码示例

#### 基本使用
```typescript
// 在场景中获取Capsule组件
const capsule = this.node.getComponent(Capsule);

// 重置位置
capsule.resetPosition();

// 移动到指定位置
capsule.moveToPosition(100);

// 获取当前位置百分比
const ratio = capsule.getPositionRatio(); // -1到1之间
```

#### 动态切换模式
```typescript
// 切换到自动移动
const capsule = this.node.getComponent(Capsule);
capsule.autoMove = true;
capsule.autoMoveSpeed = 150;

// 切换回手动控制
capsule.autoMove = false;
```

### 🎯 实际应用场景

#### 1. 平台游戏角色
- 设置`moveSpeed = 300`，`moveRange = 400`
- 使用手动控制进行游戏操作

#### 2. UI动画元素
- 设置`autoMove = true`，`autoMoveSpeed = 50`
- 创建平滑的UI动画效果

#### 3. 敌人AI巡逻
- 设置`autoMove = true`，`autoMoveSpeed = 80`
- 在敌人节点上添加此组件实现巡逻行为

### 🔍 边界处理

- 自动边界检测：胶囊体不会移出设定的移动范围
- 边界反弹：到达边界时会自动改变方向（自动模式）
- 初始位置：以添加组件时的位置为基准中心点

### 🎮 测试方法

1. **预览测试**：
   - 点击Cocos Creator的"预览"按钮
   - 使用键盘控制胶囊体移动
   - 观察边界限制效果

2. **场景测试**：
   - 创建测试场景
   - 添加胶囊体节点
   - 添加Capsule组件
   - 调整参数进行测试

### 📱 移动端适配

虽然当前实现基于键盘控制，但可以通过以下方式适配触摸控制：

```typescript
// 在触摸事件中调用移动方法
onTouchMove(event: EventTouch) {
    const capsule = this.node.getComponent(Capsule);
    const touchX = event.getLocationX();
    // 根据触摸位置计算目标位置
    capsule.moveToPosition(targetX);
}
```

### 🚨 常见问题

1. **移动无响应？**
   - 检查节点是否有碰撞体组件
   - 确认场景是否启用了输入系统
   - 查看浏览器控制台是否有错误

2. **超出边界？**
   - 检查`moveRange`参数设置
   - 确认初始位置是否正确

3. **键盘控制无效？**
   - 确保场景有输入焦点
   - 检查键盘事件是否被其他组件拦截

现在您可以直接在场景中使用这个胶囊体移动功能了！